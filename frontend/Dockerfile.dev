FROM node:18-alpine

WORKDIR /app

# package.json과 package-lock.json 복사 (캐싱 최적화)
COPY package*.json ./

# 의존성 설치
RUN npm install --legacy-peer-deps

# 개발 서버 시작을 위한 글로벌 도구 설치
RUN npm install -g nodemon

# 포트 노출
EXPOSE 3000

# 개발 모드 실행
CMD ["npm", "start"]